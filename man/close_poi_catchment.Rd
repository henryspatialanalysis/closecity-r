% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{close_poi_catchment}
\alias{close_poi_catchment}
\title{A POI's catchment (blocks that can reach it)}
\usage{
close_poi_catchment(
  client,
  dest_id,
  mode = NULL,
  block = NULL,
  max_minutes = NULL,
  limit = NULL,
  cursor = NULL
)
}
\arguments{
\item{client}{A \code{close_client()}.}

\item{dest_id}{Destination id.}

\item{mode}{Mode label(s) to filter by.}

\item{block}{Restrict to specific block id(s).}

\item{max_minutes}{Cap travel time (<= 30).}

\item{limit}{Page size (<= 1000).}

\item{cursor}{Opaque keyset cursor; normally use \code{\link[=close_records]{close_records()}} instead.}
}
\value{
A \link{close_reply}.
}
\description{
Every census block that can reach a POI, one row per (block, mode). Paginated;
loop with \code{\link[=close_records]{close_records()}}. Metered per returned row.
}
\seealso{
Other POI endpoints:
\code{\link[=close_poi]{close_poi()}},
\code{\link[=close_pois_search]{close_pois_search()}}
}
\concept{POI endpoints}
