% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabular.R
\name{close_as_df}
\alias{close_as_df}
\title{Convert a Close reply to a data.frame}
\usage{
close_as_df(x, key = NULL)
}
\arguments{
\item{x}{(\code{close_reply})\cr A reply, or the same list shape.}

\item{key}{(\code{character(1)}, default NULL)\cr Name of the record array to read.
Detected from the body when omitted.}
}
\value{
A \code{data.frame}. Summary replies gain a broadcast \code{geoid} column.
}
\description{
Turns a \code{close_reply} into a plain \code{data.frame}, one row per record. This is
what the client returns in the \code{"tabular"} output mode; \code{\link[=close_as_sf]{close_as_sf()}} adds
geometry on top of the same rows. Metering and envelope metadata (token counts,
\code{block_geoid}, \code{assumptions}, ...) are attached as attributes.
}
\examples{
\dontrun{
close <- close_client(output = 'raw')
close_as_df(close$modes())
}
}
