% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/endpoints.R
\name{close_isochrone}
\alias{close_isochrone}
\title{Travel-time contours (isochrone)}
\usage{
close_isochrone(
  client,
  block = NULL,
  lon = NULL,
  lat = NULL,
  mode = NULL,
  direction = NULL,
  minutes = NULL,
  contours = NULL,
  format = NULL,
  v = NULL,
  if_none_match = NULL
)
}
\arguments{
\item{client}{A \code{close_client()}.}

\item{block}{Origin block GEOID (or give \code{lon} + \code{lat}).}

\item{lon, lat}{Origin point (WGS84), as an alternative to \code{block}.}

\item{mode}{\code{"walk"}, \code{"bike"}, or \code{"transit"}.}

\item{direction}{\code{"to"} (blocks that can reach the origin) or \code{"from"}.}

\item{minutes}{A single threshold (1-60).}

\item{contours}{Up to 4 ascending levels (a vector or comma string), instead
of \code{minutes}.}

\item{format}{\code{"geojson"} (polygons) or \code{"blocks"} (a block list).}

\item{v}{Opaque cache-buster, echoed back.}

\item{if_none_match}{An ETag to revalidate; returns a free HTTP 304 on a match.}
}
\value{
A \link{close_reply}.
}
\description{
Travel-time contours from a \code{block} (GEOID) or \code{lon} + \code{lat}. Charged one
token per contour level (1-4), not per row. With \code{format = "geojson"} the
reply carries polygon geometry you can convert with \code{\link[=close_as_sf]{close_as_sf()}}.
}
\examples{
\dontrun{
iso <- close_isochrone(cl, block = "440070036001010", mode = "walk",
                       minutes = 15, format = "geojson")
close_as_sf(iso)
}
}
\seealso{
Other isochrone endpoints:
\code{\link[=close_isochrone_meta]{close_isochrone_meta()}}
}
\concept{isochrone endpoints}
